import SwiftUI

struct FavoritesView: View {
    var body: some View {
        PlaceholderScreen(
            titleKey: "tab.favorites",
            messageKey: "favorites.placeholder"
        )
    }
}

#Preview { FavoritesView() }
