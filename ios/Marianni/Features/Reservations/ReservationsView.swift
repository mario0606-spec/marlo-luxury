import SwiftUI

struct ReservationsView: View {
    var body: some View {
        PlaceholderScreen(
            titleKey: "tab.reservations",
            messageKey: "reservations.placeholder"
        )
    }
}

#Preview { ReservationsView() }
