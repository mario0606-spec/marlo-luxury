import SwiftUI

struct BrowseView: View {
    var body: some View {
        PlaceholderScreen(
            titleKey: "tab.browse",
            messageKey: "browse.placeholder"
        )
    }
}

#Preview { BrowseView() }
