included:
  - Marianni
  - Tests

excluded:
  - Marianni.xcodeproj
  - build
  - DerivedData

disabled_rules:
  - todo
  - line_length

opt_in_rules:
  - empty_count
  - explicit_init
  - first_where
  - sorted_imports
  - toggle_bool

identifier_name:
  min_length: 1
  excluded:
    - id
    - x
    - y
    - r
    - g
    - b
